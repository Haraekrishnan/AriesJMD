rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to read and write to the damage-reports folder
    match /damage-reports/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
